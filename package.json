{
  "name": "@shaderfrog/glsl-parser",
  "engines": {
    "node": ">=16"
  },
  "version": "0.0.2",
  "description": "A GLSL ES 1.0 and 3.0 parser that can preserve whitespace and comments",
  "scripts": {
    "prepare": "npm run build",
    "build": "mkdir -p dist && pegjs -o dist/parser.js peg/glsl-pegjs-grammar.pegjs",
    "watch-test": "jest --watch",
    "test": "jest"
  },
  "files": [
    "dist/parser.js"
  ],
  "jest": {
    "moduleFileExtensions": [
      "js",
      "json",
      "pegjs",
      "glsl"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ShaderFrog/glsl-parser.git"
  },
  "keywords": [
    "glsl",
    "parser",
    "shaderfrog"
  ],
  "author": "Andrew Ray",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ShaderFrog/glsl-parser/issues"
  },
  "homepage": "https://github.com/ShaderFrog/glsl-parser#readme",
  "devDependencies": {
    "jest": "^27.0.2",
    "pegjs": "^0.10.0",
    "prettier": "^2.1.2"
  }
}
